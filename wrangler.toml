name = "worddrop"
main = "src/index.js"
compatibility_date = "2026-05-06"

[env.production]
name = "worddrop"
routes = [{ pattern = "worddrop.pages.dev", zone_name = "pages.dev" }]

[build]
command = ""
cwd = "./"
watch_paths = []

[build.upload]
format = "service-worker"

# Cloudflare Pages 配置
[pages]
build_output_dir = "."
